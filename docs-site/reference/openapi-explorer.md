# OpenAPI Explorer

This page renders the current `openapi.yaml` contract directly from this repository.

Use it to inspect request/response schemas, status codes, and examples.

<OpenApiExplorer />

## Notes

- If the explorer does not render, hard refresh once to clear cached assets.
- `Try it out` operations are schema helpers; they do not call a live API unless you point to one.
- Runtime docs are also available at service endpoint `GET /docs`.
